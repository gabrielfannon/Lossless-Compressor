#include "RandomNumberGenerator.h"
#include <iostream>
#include <string>

void static random_number(int& reference, int max_range){

    //srand(unsigned int(time(NULL))); //seed random number generator
    //reference = rand() % max_range; //writes random number

};